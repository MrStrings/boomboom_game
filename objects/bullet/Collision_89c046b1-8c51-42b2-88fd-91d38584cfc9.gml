/// @description


//move_contact_solid(direction, 10000);
/*while (place_meeting(x, y, wall)) {
    x -= (cos(direction)*0.05);
    y -= (sin(direction)*0.05);
}*/

//speed = 0;

//instance_create(x, y, explosion_controller);
//instance_create(0,0, screen_shake);
//instance_destroy();